package processor;

import models.Node;

import java.util.List;

public class OnProcessor implements BoardProcessor {
    @Override
    public int compute(List<Node> inputs) {
        return 1;
    }
}
