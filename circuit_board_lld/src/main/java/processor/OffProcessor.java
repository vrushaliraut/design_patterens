package processor;

import models.Node;

import java.util.List;

public class OffProcessor implements BoardProcessor {
    @Override
    public int compute(List<Node> parents) {
        return 0;
    }
}
